  The bios starts the boot process and provides
  some setup features.  It is a "real mode" program
  that is in ROM (read only memory).  It saves
  setup information in nvram or <cmos> RAM.  Newer
  bios's use the plug-and-play standard and we
  can find them by searching for the string "_DMI_".

